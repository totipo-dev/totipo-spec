# Keep source and configuration formatting stable on every checkout platform.
* text=auto eol=lf

# Reviewed artifacts and manifests are hashed as exact bytes. Never convert them.
/vectors/** -text
/review/** -text
/requirements/*.json -text
/requirements/*.sha256 -text
